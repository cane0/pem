import * as React from "react";
import { StyleSheet, View, Image } from "react-native";
const logo = require("../assets/powered_by_google_on_white.png");
export function ListFooter({ style }) {
    return (React.createElement(View, { style: [styles.contianer, style] },
        React.createElement(Image, { source: logo, style: styles.logo })));
}
const styles = StyleSheet.create({
    contianer: {
        padding: 10,
        justifyContent: "flex-end",
        flexDirection: "row",
    },
    logo: {
        width: 150,
        height: 20,
        resizeMode: "contain",
    },
});
//# sourceMappingURL=ListFooter.js.map